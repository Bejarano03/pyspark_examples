# PySpark Examples

## Description

This script goes over a few examples
of using PySpark. It creates a session,
a dataframe and manipulates it via the API
and using SQL commands. It then parallelizes
a text file and displays a word count from
the txt file.

## Installation

- Clone repo to local environment
- Install via pip: PySpark and psutil
- Run script on local console
